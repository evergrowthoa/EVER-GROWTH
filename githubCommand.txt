ㅁㅁ 올앤업에 패스접수요청칸 만들기
ㅁㅁ 접수되면 자동발송된다고 필수안내하기
ㅁㅁ 중간에 오류(결제정보/주소지/일정/후결합/9개월재접수/선접수/물마크중복) 등 생길경우 중단 후 알림
ㅁㅁ 패스 or 전자서명 발송시 담당자한테 어떻게 전달할건지 (기존처럼 타이핑?)
ㅁㅁ 매트리스(경도,프레임) 청정기(필터선택) 연수기(벽걸이형 스탠드형)


Git 사용자 설정 (최초 1회만)

⚠️ 이건 무조건 한 번만 하면 됨

git config --global user.name "네 이름"
git config --global user.email "네 깃허브이메일"

예:
git config --global user.name "evergrowthoa"
git config --global user.email "example@gmail.com"
==============================================
프로젝트 처음 가져오기 (클론)
📌 원하는 위치로 이동

예: 바탕화면

cd Desktop

또는 정확하게:

cd C:\Users\사용자이름\Desktop
📌 깃허브 프로젝트 복사 (최초 1회)
git clone https://github.com/evergrowthoa/EVER-GROWTH.git

설명:

clone = 깃허브 프로젝트를 통째로 복사

자동으로 EVER-GROWTH 폴더 생성됨
===============================================
앞으로 자주 쓰는 Git 명령어 정리
🔹 현재 상태 확인
git status
🔹 변경 파일 추가
git add .
🔹 커밋
git commit -m "설명 적기"
🔹 깃허브로 올리기
git push origin main
🔹 다른 PC에서 최신버전 가져오기
git pull origin main
===============================================
작업 기본 루틴 (앞으로 계속 쓰는 패턴)
📌 코드 수정 후
git add .
git commit -m "수정 내용"
git push origin main
📌 다른 PC에서 작업 시작할 때
git pull origin main